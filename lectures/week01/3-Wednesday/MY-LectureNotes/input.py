# create a variable called name. Set the value of name to input received from user.
name = "Thomas"
# create a variable called age, set the value to the age of given by the user input.
age = 27
# check the type of the age (must use the print function)
print(type(age))
# convert the age value to an integer 
num = int(age)
# print the year the user was born (2022 - age)
print(2022 - age)
#check the type of the following (you have to use to print statement to see the results)


isValid = "true"

isValid2 = True 

myNum = 3 

myName = "Larry"

print(type(isValid), type(isValid2), type(myNum), type(myName))